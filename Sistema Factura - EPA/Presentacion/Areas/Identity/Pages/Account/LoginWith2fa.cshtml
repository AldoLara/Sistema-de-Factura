@page
@model LoginWith2faModel
@{
    ViewData["Title"] = "Autenticación de dos factores";
}

<h2>@ViewData["Title"]</h2>
<hr />

    <body class="sectionimg">
    <form method="post" asp-route-returnUrl="@Model.ReturnUrl" class="formulario">
        <h2>@ViewData["Title"]</h2>
        <p>Su inicio de sesión está protegido con una aplicación de autenticación. Ingrese su código de autenticación a continuación.</p>

            <input asp-for="RememberMe" type="hidden" />
            <div asp-validation-summary="All" class="text-danger"></div>
        <div class="contenedor">
            <div class="input-contenedor">
                <i class="fas fa-code icon"></i>
                <input asp-for="Input.TwoFactorCode" class="inputTipo" placeholder="Código" autocomplete="off" />
            </div>
            <div class="form-group">
                <div class="checkbox">
                    <label asp-for="Input.RememberMachine">
                        <input asp-for="Input.RememberMachine" />
                        @Html.DisplayNameFor(m => m.Input.RememberMachine)
                    </label>
                </div>
            </div>
            <input type="submit" value="Iniciar sesión" class="button">
        </div>
        <p>
            ¿No tienes acceso a tu dispositivo autenticador? Puede
            <a asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">iniciar sesión con un código de recuperación</a>.
        </p>

        </form>
    </body>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}